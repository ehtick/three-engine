// Re-export: the landform generator lives with the Terrain component (P1-T),
// and since 09-14 it is the style-driven landscape (docs/TERRAIN_PLAN.md).
export { createTerrainShape, TERRAIN_STYLES } from '../terrain/proceduralTerrain.js';
