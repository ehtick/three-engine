# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-player
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.codex/
.claude/
.cursor/
.vscode/

/dist
/dist-player

package-lock.json

# Diagnostic output written by the harnesses in scripts/ (screenshots, probe
# dumps, perf traces). Always regenerable by re-running the harness, so it is
# never committed. `npm run clean:artifacts` deletes it.
scripts/*.png
# Generated repro project (scripts/lib/makeCornellProject.mjs) — rewritten on
# every run-gi-emissive.mjs run, never hand-edited.
scripts/.gi-cornell/
scripts/*.jpg
scripts/*.jpeg
scripts/*.webp
scripts/*.gif
/artifacts/